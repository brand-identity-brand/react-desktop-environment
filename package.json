{
  "name": "react-desktop-environment-monorepo",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace @react-desktop-environment/demo",
    "build": "npm run build --workspace react-desktop-environment && npm run build --workspace @react-desktop-environment/demo",
    "test": "npm run test --workspace react-desktop-environment"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/brand-identity-brand/react-desktop-environment.git"
  },
  "license": "ISC"
}
